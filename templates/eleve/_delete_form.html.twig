<a href="{{ path('eleve_edit', {'idEleve': eleve.idEleve}) }}" class="btn btn-primary mr-1 waves-effect waves-light"><i class="feather icon-edit-1"></i> Edit</a>
<form method="post" action="{{ path('eleve_delete', {'idEleve': eleve.idEleve}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eleve.idEleve) }}">
    <br>
    <button class="btn btn-outline-danger waves-effect waves-light"><i class="feather icon-trash-2"></i> Delete </button>
</form>
