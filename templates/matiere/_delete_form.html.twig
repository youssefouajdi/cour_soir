<form method="post" action="{{ path('matiere_delete', {'idMatiere': matiere.idMatiere}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matiere.idMatiere) }}">
    <button class="btn">Delete</button>
</form>
